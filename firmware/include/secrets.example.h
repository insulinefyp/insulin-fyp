#pragma once

// Copy this file to secrets.h and fill in your own network.
// secrets.h is git-ignored and must never be committed.

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-network-password"

// Name the device advertises on the local network (reachable as
// <name>.local from machines that support mDNS).
#define DEVICE_HOSTNAME "insulin-pump"
